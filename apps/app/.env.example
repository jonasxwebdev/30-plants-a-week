# Environment variables for app

# Supabase (PUBLIC_ prefix required for client-side access)
PUBLIC_SUPABASE_URL=your-project-url.supabase.co
PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# App URL (for redirect URLs)
VITE_APP_URL=http://localhost:4321

# Vercel Cron Secret (generate a random string)
CRON_SECRET=your-cron-secret
